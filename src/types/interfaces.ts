export interface location{
    query: string
}

export interface propsSearchResult {
    username: string
}

export interface propsSuggestUser {
    username: string,
    profileImage: string,
    api_url: string
}